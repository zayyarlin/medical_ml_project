version: '2'

services:
  website:
    build: .
    environment:
      PYTHONUNBUFFERED: 'true'
      PORT: 80
      STRIPE_SECRET_KEY: ''
      STRIPE_PUBLISHABLE_KEY: ''
    volumes:
      - '.:/app'
    ports:
      - '80:80'
